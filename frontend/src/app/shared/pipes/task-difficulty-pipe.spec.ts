import { TaskDifficultyPipe } from './task-difficulty-pipe';

describe('TaskDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
